

DB_HOST=your-database-host
DB_PORT=5432
DB_NAME=coral_monitoring
DB_USER=your_username
DB_PASS=your_password

# Mapbox token (if using Mapbox tiles)
MAPBOX_TOKEN=your_mapbox_token

# App configuration
DEBUG=True
SECRET_KEY=your-secret-key-for-sessions